import { Dimensions } from 'react-native'
const { width, heigth } = Dimensions.get('window')

export const COLORS = {
    // basic colors
    primary : '#FA4248',
    secondary : '',

    dark : ''
}

export const SIZES = {

}